name: Ci Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on:ubuntu-latest
  
  steps:
    
    - name: Set up Node.js
      with:
        node-version:22
    
    - name: Install dependencies
      run: npm install
    
    - name: Run tests
      run: npm test